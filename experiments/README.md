# 实验目录

实验脚本位于 `scripts/evaluation/`，默认输出到：

```text
experiments/results/
```

`results/` 已加入 `.gitignore`，用于保存评估 CSV、截图和图表等运行产物。

当前默认上位机算法由 `docs/reports/直播场景模拟测试报告.md` 决定；最近一次真实人像直播压力测试显示 RVM 综合表现最好，因此应用默认已切换到 RVM。
